# A股量化分析工具 v2 依赖
# 核心数据处理
pandas>=1.5.0
numpy>=1.21.0

# HTTP请求
requests>=2.28.0
tenacity>=8.0.0

# 数据源（兜底）
akshare>=1.2.0

# Streamlit Web界面（保底方案）
streamlit>=1.30.0
plotly>=5.18.0

# 新的 Web API + 前端服务
fastapi>=0.110.0
uvicorn>=0.25.0

# 机器学习（可选，用于权重优化）
scikit-learn>=1.3.0

# 通知
# feishu webhook 仅需 requests，无额外依赖
